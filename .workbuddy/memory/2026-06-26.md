# 2026-06-26 工作日志

## CircularProgress 环形进度盘不更新 BUG 修复（第三轮 — 真正根因）

### 问题
用户反馈：答题完成目标后返回首页，环形进度条和"已完成"数字均无变化。第二轮修复（onShow redraw）未解决问题。

### 根因分析（通过日志定位）
日志文件 `127.0.0.1-1782403075957.log` 中反复出现 6 次：
```
TypeError: requestAnimationFrame is not a function
    at CircularProgress.js
```

**微信小程序中没有全局 `requestAnimationFrame` / `cancelAnimationFrame`**。它们是 Canvas 2D 节点的方法：`canvasNode.requestAnimationFrame(cb)` / `canvasNode.cancelAnimationFrame(id)`。

代码中直接调用了全局 `requestAnimationFrame(frame)`，导致：
1. `initCanvas` → `drawRing` → `frame()` → `requestAnimationFrame(frame)` → 报错 → Canvas 完全空白
2. `watch` 回调 → `drawRing` + `animateValue` → 都调用全局 RAF → 都报错
3. `onShow` → `redraw()` → 仍然调用全局 RAF → 仍然报错

第二轮修复（onShow redraw）方向正确但无法生效，因为 redraw 内部同样调用了不存在的全局 RAF。

### 修复方案
在 `src/components/CircularProgress.vue` 中新增兼容封装函数：
- `raf(callback)`：优先 `canvasNode.requestAnimationFrame`，降级 `setTimeout(cb, 1000/60)`
- `caf(id)`：优先 `canvasNode.cancelAnimationFrame`，降级 `clearTimeout(id)`

替换全部 4 处调用：
1. drawRing 中 `cancelAnimationFrame(animFrameId)` → `caf(animFrameId)`
2. drawRing 中 `requestAnimationFrame(frame)` → `raf(frame)`
3. animateValue 中 `cancelAnimationFrame(valueAnimFrameId)` → `caf(valueAnimFrameId)`
4. animateValue 中 `requestAnimationFrame(frame)` → `raf(frame)`

### 构建验证
✅ `npm run build:mp-weixin` 构建成功（Exit Code: 0）

## 刷题页重构 — 高级微立体与现代通透风格升级

### 目标
将刷题页视觉风格与首页/设置页统一为"高级微立体与现代通透"风格。

### 修改文件

**1. `src/styles/tokens/_colors.scss`** — 新增 9 个设计令牌：
- 题型标签胶囊色：`$color-tag-single-bg`(rgba(112,72,182,0.08))、`$color-tag-multiple-bg`、`$color-tag-judge-bg`
- 选项三级状态色：`$color-option-bg`(#F6F6F9)、`$color-option-border`、`$color-option-marker-bg`(#EBEBF0)、`$color-option-selected-bg`、`$color-option-selected-border`、`$color-option-selected-marker-shadow`(内阴影)
- 按钮阴影：`$shadow-btn-confirm`(0 8rpx 24rpx rgba(74,71,163,0.3))

**2. `src/components/QuestionCard.vue`** — 模板+样式重构：
- 删除冗余 `question-number` 显示 + `questionNumber`/`total` Props
- 题型标签：半透明胶囊样式（rgba 背景 + 加深品牌色 + letter-spacing）
- 题目文本：line-height 1.6 + margin-bottom → $space-2xl(48rpx)
- 卡片阴影：`0 12rpx 40rpx rgba(0,0,0,0.03)` 微立体悬浮
- 选项默认态：#F6F6F9 + 1px 极淡边框 + 微阴影
- 选项选中态：rgba(74,71,163,0.04) 淡紫背景 + 半透明紫边框
- 选项标记圆圈：渐变深紫($gradient-primary) + 白色文字 + inset 内阴影
- 过渡动画：`all 0.2s ease` + 点击 `scale(0.98)` 反馈

**3. `src/pages/quiz/index.vue`** — 模板+样式重构：
- 移除传给 QuestionCard 的 question-number/total props
- 进度条：8rpx 高 + 4rpx 圆角 + $gradient-primary 渐变填充
- 底部栏：`position: fixed` 固定悬浮 + 毛玻璃渐变背景(white 0%→85%→95%) + `backdrop-filter: blur(20px)` + safe-area 适配
- "上一题"按钮：`variant="outline"` 线框风格
- 主操作按钮（确认答案/下一题/提交）：紫色外阴影 `$shadow-btn-confirm`
- scroll 区域：`padding-bottom: 160rpx` 为固定栏留空

### 构建验证
✅ `npm run build:mp-weixin` 构建成功，零编译错误

## 像素级细节打磨（4 项 UI 优化）

### Bug：确认答案按钮圆角露白
- **根因**：微信小程序 `<button>` 默认 `::after` 伪元素带边框，覆盖在圆角处形成白色矩形死角
- **修复**：
  - `BaseButton.vue` — `.base-btn::after { border: none; }` 全局重置
  - `quiz/index.vue` — `.action-btn { overflow: hidden; height: 88rpx; box-sizing: border-box; }`

### 题型标签形状调整
- **QuestionCard.vue** — `.type-tag` 圆角从 `$radius-full`(胶囊) 改为 `$radius-sm`(8rpx 圆角矩形)；padding 从 `6rpx` 调为 `8rpx`
- **间距** — `.question-type` margin-bottom 从 `$space-lg`(24rpx) → `$space-xl`(32rpx)，题目文本与标签呼吸感增强

### 进度条升级
- 高度从 `8rpx` → `6rpx`（更精致）
- 轨道背景从 `$color-border-light` → `rgba(74, 71, 163, 0.08)`（极淡灰紫色，与主色调统一）

### 底部控制栏统一
- `.action-bar` 新增顶部向上投影 `box-shadow: 0 -4rpx 20rpx rgba(0, 0, 0, 0.02)`
- `.action-btn` 统一 `height: 88rpx` + `box-sizing: border-box` + `overflow: hidden`
- 构建通过 ✅

## 修复：下一题按钮圆角露白 + 阴影截断 Bug

### 根因
上一轮为修复圆角露白，在 `.action-btn`（view 包裹层）上加了 `overflow: hidden`。这导致了两个问题：
1. **圆角露白未根治** — `::after { border: none }` 加在 view 上无效（实际 button 在内部）
2. **box-shadow 被截断** — `overflow: hidden` 裁剪了主按钮的 `$shadow-btn-confirm` 紫色外阴影

### 修复
- **quiz/index.vue** — `.action-btn` 移除 `overflow: hidden` 和无效的 `&::after` 重置
- **BaseButton.vue** — `.base-btn` 增强：
  - `transform: translateZ(0)` 硬件加速，提升至独立合成层
  - `&::after { border: none; display: none; }` 彻底隐藏伪元素（从源头根治露白）
- 构建通过 ✅
